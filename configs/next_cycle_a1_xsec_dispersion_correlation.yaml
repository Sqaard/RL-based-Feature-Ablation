experiment_name: next_cycle_a1_xsec_dispersion_correlation
status: ready_to_run

reference_baseline:
  config_name: custom_custom
  algorithm: PPO
  reward_mode: custom_reward
  policy_mode: custom_mlp_policy
  checkpoint_selection_rule: checkpoint_robust_score
  configuration_selection_rule: robust_q25_retention
  buy_cost_pct: 0.001
  sell_cost_pct: 0.001
  action_constraint: long_only_box_[-1,1]_scaled_by_hmax

next_cycle_candidate:
  family_id: xsec_dispersion_correlation_regime
  feature_set_name: base_macro_xsec_dispersion_correlation_regime
  source_type: derivable_from_existing_panel
  external_data_required: false
  notes: Use this as the first A1 candidate because it is implemented causally from the existing Dow 30 panel.

required_feature_sets:
  - base_macro
  - base_macro_hmm
  - base_macro_gru
  - base_macro_xsec_dispersion_correlation_regime

optional_after_full_reporting:
  feature_sets:
    - base
  extra_seeds:
    - 2024
    - 2025

runner_kwargs:
  base_config_name: custom_custom
  candidate_feature_families:
    - xsec_dispersion_correlation_regime
  feature_set_filter:
    - base_macro
    - base_macro_hmm
    - base_macro_gru
    - base_macro_xsec_dispersion_correlation_regime
  seeds:
    - 42
    - 123
    - 999
  total_timesteps: 200000
  max_folds: null
  es_mode: relaxed
  dropout_p: 0.1
  eval_freq: 8192
  checkpoint_freq: 4096
  verbose: 0

benchmark_requirements:
  benchmark_suite_daily_expected: true
  primary_benchmark_id: dow30_equal_weight_rebalance_matched
  same_walk_forward_windows_as_agent: true
  same_cost_convention_as_agent: true

forbidden_in_same_cycle:
  - multi_family_bundle
  - architecture_change
  - latent_action_training
  - ssl_training
  - state_compression_training
  - benchmark_definition_change_mid_comparison
