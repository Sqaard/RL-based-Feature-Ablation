experiment_name: next_cycle_candidate_only_xsec_sector_gated_context
status: ready_to_run
launch_selection:
  candidate_family: xsec_sector_gated_context
  panel_scope: candidate_only
reference_baseline:
  config_name: custom_custom
  algorithm: PPO
  reward_mode: custom_reward
  policy_mode: custom_mlp_policy
  checkpoint_selection_rule: checkpoint_robust_score
  configuration_selection_rule: robust_q25_retention
  buy_cost_pct: 0.001
  sell_cost_pct: 0.001
  action_constraint: long_only_box_[-1,1]_scaled_by_hmax
notes:
  execution_mode: Run only the xsec_sector_gated_context diagnostic candidate family
    on this notebook or worker.
  merge_strategy: Merge the resulting output directory with existing baseline and
    candidate outputs later.
  baseline_reuse_expected: true
  diagnostic_intent: Test whether the stable xsec regime signal can gate the episodic
    sector-relative signal.
runner_kwargs:
  base_config_name: custom_custom
  seeds:
  - 42
  - 123
  - 999
  total_timesteps: 200000
  max_folds: null
  es_mode: relaxed
  dropout_p: 0.1
  eval_freq: 8192
  checkpoint_freq: 4096
  verbose: 0
