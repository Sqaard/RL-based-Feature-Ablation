experiment_name: next_cycle_candidate_only_rates_term_structure_lsc
status: ready_to_run
launch_selection:
  candidate_family: rates_term_structure_lsc
  panel_scope: candidate_only
reference_baseline:
  config_name: custom_custom
  algorithm: PPO
  reward_mode: custom_reward
  policy_mode: custom_mlp_policy
  checkpoint_selection_rule: checkpoint_robust_score
  configuration_selection_rule: robust_q25_retention
  buy_cost_pct: 0.001
  sell_cost_pct: 0.001
  action_constraint: long_only_box_[-1,1]_scaled_by_hmax
notes:
  execution_mode: Run only the rates_term_structure_lsc candidate family on this notebook
    or worker.
  dataset_requirement: Use processed_final_fixed_external_lagclean_full.csv.
  merge_strategy: Merge the resulting output directory with existing baseline and
    candidate outputs later.
  baseline_reuse_expected: true
runner_kwargs:
  base_config_name: custom_custom
  seeds:
  - 42
  - 123
  - 999
  total_timesteps: 200000
  max_folds: null
  es_mode: relaxed
  dropout_p: 0.1
  eval_freq: 8192
  checkpoint_freq: 4096
  verbose: 0
